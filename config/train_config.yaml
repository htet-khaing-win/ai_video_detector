model:
  name: "baseline_cnn"
  architecture: "resnet18"
  num_classes: 2
  pretrained: true
  temporal_pool: "avg"

data:
  train_csv: "data/splits/train_crossgen.csv"
  val_csv: "data/splits/val_crossgen.csv"
  cache_root: "data/processed/genbuster_cached"
  batch_size: 32
  num_workers: 4
  max_frames: 8

training:
  epochs: 60
  learning_rate: 0.0005
  optimizer: "adamW"
  weight_decay: 0.02
  
  # Early stopping
  early_stopping:
    enabled: true
    patience: 10
    monitor: "val_acc"
    mode: "max"
  
  # Learning rate scheduler
  lr_scheduler:
    enabled: true
    type: "reduce_on_plateau"
    factor: 0.5
    patience: 5
    min_lr: 0.000001
    monitor: "val_loss"
    mode: "min"

checkpoint:
  save_dir: "models"
  save_best_only: true
  monitor: "val_acc"
  mode: "max"

device: "cuda"

logging:
  log_dir: "logs"
  plot_dir: "plots"
  save_plot: true